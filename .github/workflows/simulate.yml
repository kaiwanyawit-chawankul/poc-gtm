name: Simulate traffic

on:
  schedule:
    - cron: '0 8 * * *'
    - cron: '0 12 * * *'
    - cron: '0 18 * * *'
  workflow_dispatch: {}

jobs:
  run-simulate:
    name: Run simulate.js
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. Enable Corepack first so the pnpm command is available
      - name: Enable Corepack
        run: |
          corepack enable
          corepack prepare pnpm@latest --activate

      # 2. Setup Node and configure caching now that pnpm exists
      - name: Setup Node.js and pnpm
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: tests/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install
        working-directory: tests

      - name: Run simulation
        run: node simulate.js
        working-directory: tests
